
import { quantityPrice } from 'bookstore/helpers/quantity-price';
import { module, test } from 'qunit';

module('Unit | Helper | quantity price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = quantityPrice([42]);
  assert.ok(result);
});

