<div class="row">
  <div class="col-sm-6">
    <h3>Add new item</h3>
    {{new-item saveItem='saveItem'}}
  </div>
  <div class="col-sm-6">
    <h3>Update item:</h3>
    <ul>
      {{#each model as |item|}}
        {{update-item item=item}}
      {{/each}}
    </ul>
  </div>
</div>
