<div class="container">
  <nav>
    <div class="navbar-left">
      {{#link-to 'index'}}
        {{fa-icon 'book'}}
      {{/link-to}}
      |
      {{#link-to 'store'}}
        Store
      {{/link-to}}
      | {{#link-to 'admin'}}Admin{{/link-to}}
    </div>
    <div class="navbar-right">
      {{cart-number}}
    </div>
  </nav>
</div>
<div class="container">
{{outlet}}
</div>
